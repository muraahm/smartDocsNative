import React from 'react';
import {Header, Left, Body, Right, Title} from 'native-base';

const AppHeader = () => {
  return (
    <Header>
      <Left />
      <Body>
        <Title>Header</Title>
      </Body>
      <Right />
    </Header>
  );
};

export default AppHeader;
